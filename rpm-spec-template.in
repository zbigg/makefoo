#
# This is a spec file for component-unix
# that assumes that binaries 
#   ARE ALREADY INSTALLED in
#   BUILDROOT (staging dir of rpmbuild)
#
# See Makefile.in/rpm-dist target
#

Summary:    @PRODUCT@ - @COMPONENT@
License:    proprietary
Name:       @COMPONENT@
Version:    @VERSION@
Release:    @RELEASE@
Group:      System Environment/Daemons

%description
@@DESCRIPTION@@

# those can be safely skipped because we assume
# that component is already installed in BUILDROOT
%prep
%build
%install

#%post
#
#PREFIX=%{_prefix}
#CONFIG_DIR=%{_sysconfdir}
#VAR_DIR=%{_localstatedir}
#LIB_DIR=%{_libdir}
#export PREFIX CONFIG_DIR VAR_DIR LIB_DIR
#
#%{_libdir}/component/component-setup postinstall

#%preun
#PREFIX=%{_prefix}
#CONFIG_DIR=%{_sysconfdir}
#VAR_DIR=%{_localstatedir}
#LIB_DIR=%{_libdir}
#export PREFIX CONFIG_DIR VAR_DIR LIB_DIR
#
#%{_libdir}/component/component-setup preremove

%files
%defattr(-,root,root)
%{_prefix}
#%{_bindir}/*
#%{_sysconfdir}/*
#%{_defaultdocdir}/*
#%{_libdir}/*
#%{_localstatedir}/*

# jedit: :tabSize=8:mode=rpm-spec:

